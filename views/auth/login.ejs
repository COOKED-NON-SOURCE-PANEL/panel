<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name %> - Login</title>
  <link href="../assets/tailwind.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
</head>

<body class="h-full bg-[#10181e] text-white p-12 font-[general-sans]">
    <div id="content">
       <section class="relative py-5 2xl:py-5 overflow-hidden">
          <div class="container px-4 mx-auto">
             <div class="max-w-7xl mx-auto">
                <div class="flex flex-wrap -mx-4">
                   <!-- Left Image Section -->
                   <div class="w-full lg:w-1/2 px-4 order-last lg:order-first">
                      <div class="relative max-w-lg mx-auto lg:mx-0 lg:max-w-2xl h-full">
                         <img class="block w-full h-142 sm:h-full object-cover rounded-5xl" src="https://raw.githubusercontent.com/NinoviumLabs/palactyl/main/assets/v2LB.png" alt="Login Image" style="border-radius: 3rem;">
                         <div class="absolute bottom-0 w-full left-0 h-full flex items-center justify-center p-10">
                            <div class="max-w-md mx-auto">
                               <h4 class="font-bold text-3xl sm:text-5xl lg:text-6xl text-white mb-8">Sign in to your <%= settings.name %> account.</h4>
                               <p class="text-lg text-zinc-400 mb-20">Welcome back! Login to your account using your username and password.</p>
                            </div>
                         </div>
                      </div>
                   </div>

                   <!-- Right Form Section -->
                   <div class="w-full lg:w-1/2 px-4 mb-16 lg:mb-0 border border-white/5 rounded-3xl bg-[#1a242f]">
                      <div class="max-w-lg mx-auto lg:pt-32 2xl:pt-64 lg:pb-8">
                         <h3 class="text-4xl sm:text-3xl text-white font-medium tracking-tight mb-3">Continue to Dashboard</h3>
                         <p class="text-md text-zinc-400 mb-8">Enter your credentials to log in.</p>
                         
                         <!-- Login Form -->
                         <form method="GET" action="/auth/login" class="mt-12">
                            <div class="mb-4">
                               <label for="username" class="text-sm font-semibold text-white">Username</label>
                               <input type="text" id="username" name="username" class="w-full mt-2 p-3 rounded-xl bg-[#1a242f] text-white placeholder-gray-500 focus:ring-2 focus:ring-[#0a9875]" placeholder="Enter your username" required />
                            </div>
                            <div class="mb-6">
                               <label for="password" class="text-sm font-semibold text-white">Password</label>
                               <input type="password" id="password" name="password" class="w-full mt-2 p-3 rounded-xl bg-[#1a242f] text-white placeholder-gray-500 focus:ring-2 focus:ring-[#0a9875]" placeholder="Enter your password" required />
                            </div>
                            <button type="submit" class="relative group block w-full py-3 px-5 text-center text-sm font-semibold text-white bg-[#0a9875] rounded-2xl overflow-hidden hover:bg-[#0a9875] transition duration-300">
                               <div class="absolute top-0 right-full w-full h-full bg-[#0a9875] transform group-hover:translate-x-full group-hover:scale-102 transition duration-150"></div>
                               <span class="relative">Login</span>
                            </button>
                            <% if (settings.register) { %>
                            <div class="mt-4 text-center">
                               <span class="text-xs font-semibold text-white">
                                  Don't have an account? <a href="/register" class="text-[#0a9875]">Create one!</a>
                               </span>
                            </div>
                            <% } %>
                         </form>
                      </div>
                   </div>
                </div>
             </div>
          </div>
       </section>
    </div>
</body>
</html>
